apiVersion: v1
kind: Secret
metadata:
  name: thuso-secret
  namespace: {{ .Values.global.namespace }}
data:
  {{- range $key, $value := .Values.secrets }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
